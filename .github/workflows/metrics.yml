name: Update Metrics

on:
  schedule:
    # Update setiap hari jam 00:00 UTC (07:00 WIB)
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Generate metrics
        uses: lowlighter/metrics@v3.32
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          github_user_name: tiarasani-anita
          plugin_languages: yes
          plugin_languages_skip_detection: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repo: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_details: lines, bytes-size, percentage
          plugin_languages_indepth: yes
          plugin_languages_limit: 8
          plugin_languages_sections: most-used, categories
          config_output: markdown